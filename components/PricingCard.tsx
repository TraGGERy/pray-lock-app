import { Pressable,Text,View } from 'react-native';
export default function PricingCard({title,price,badge,selected,onPress}:{title:string;price:string;badge?:string;selected?:boolean;onPress:()=>void}){return <Pressable onPress={onPress} className={`flex-1 p-4 rounded-2xl border ${selected?'border-[#C89B3C]':'border-[#E8DDCC]'} bg-[#FFFDF8]`}><Text className='text-[#123524] text-lg'>{title}</Text><Text className='text-3xl text-[#1E1E1E] font-semibold mt-2'>{price}</Text>{badge?<View className='self-start mt-2 bg-[#E7C879] px-2 py-1 rounded-full'><Text>{badge}</Text></View>:null}</Pressable>}
